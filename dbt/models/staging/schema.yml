version: 2

models:
  - name: stg_jumia
    description: "Cleaned and typed records from jumia.ma."
    columns:
      - name: product_sk
        description: "MD5 surrogate key on (name, price, scraped_at, source_site)."
        data_tests: [unique, not_null]
      - name: brand
        data_tests: [not_null]
      - name: model
        data_tests: [not_null]
      - name: price
        data_tests:
          - not_null
          - dbt_utils.expression_is_true:
              expression: "> 0"
      - name: source_site
        data_tests:
          - not_null
          - accepted_values:
              values: ["jumia"]
      - name: scraped_at
        data_tests: [not_null]

  - name: stg_electroplanet
    description: "Cleaned and typed records from electroplanet.ma."
    columns:
      - name: product_sk
        data_tests: [unique, not_null]
      - name: brand
        data_tests: [not_null]
      - name: price
        data_tests:
          - not_null
          - dbt_utils.expression_is_true:
              expression: "> 0"
      - name: source_site
        data_tests:
          - accepted_values:
              values: ["electroplanet"]

  - name: stg_amazon
    description: "Cleaned and typed records from amazon.fr."
    columns:
      - name: product_sk
        data_tests: [unique, not_null]
      - name: brand
        data_tests: [not_null]
      - name: price
        data_tests:
          - not_null
          - dbt_utils.expression_is_true:
              expression: "> 0"
      - name: source_site
        data_tests:
          - accepted_values:
              values: ["amazon"]
